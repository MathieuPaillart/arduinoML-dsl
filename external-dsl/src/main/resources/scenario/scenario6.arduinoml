application lcdDo

# Declaring Bricks
sensor button: 9
actuator led : 10
lcd ecranLCD: 2
keyboard kb

# Declaring States
-> write {
  ecranLCD print kb #Ecran lcd prend la valeur du keyboard
  led <= kb #Led prend la valeur HIGH si on tape "mathieu" sinon la valeur low si on tape seg => comportement par défaut 1/0
  => write
}